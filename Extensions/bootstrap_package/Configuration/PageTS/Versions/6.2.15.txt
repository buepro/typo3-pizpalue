mod.wizards.newContentElement.wizardItems.common {
	show := removeFromList(textmedia)
	show := addToList(text,textpic)
}

TCEFORM.tt_content.CType.removeItems = textmedia


/**
 * add styles to content element
 *
 */
TCEFORM.tt_content.section_frame {
    addItems {
        100 = Equal image column height
    }
}