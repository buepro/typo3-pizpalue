#=========================================================#
# This script needs to be loaded manually
#=========================================================#

pizpalue {
    agency.siteMode = 1
    features.revealFooter = 1
}

page.theme {
    cookieconsent {
        enable = 1
        position = bottom
        static = 0
        layout = basic
    }

    navigation {
        style = default
        type = top
    }
}
